<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Patient Details</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">

  <div class="text-center my-8">
    <h1 class="text-3xl font-bold">Patient Details</h1>
  </div>

  <div class="max-w-2xl mx-auto bg-white p-6 rounded-lg shadow-md">
    <table class="w-full table-auto border-collapse">
      <tbody>
        <tr class="border-b">
            <th class="text-left py-2 pr-4 font-medium">ID</th>
            <td class="py-2"><%= patient.id %></td>
        </tr>
        <tr class="border-b">
            <th class="text-left py-2 pr-4 font-medium">Name</th>
            <td class="py-2"><%= patient.name %></td>
        </tr>
        <tr class="border-b">
            <th class="text-left py-2 pr-4 font-medium">Contact Details</th>
            <td class="py-2"><%= patient.contactDetails %></td>
        </tr>
        <tr class="border-b">
            <th class="text-left py-2 pr-4 font-medium">Address</th>
            <td class="py-2"><%= patient.address %></td>
        </tr>
        <tr class="border-b">
            <th class="text-left py-2 pr-4 font-medium">Email</th>
            <td class="py-2"><%= patient.email %></td>
        </tr>
        <tr class="border-b">
            <th class="text-left py-2 pr-4 font-medium">Gender</th>
            <td class="py-2"><%= patient.gender %></td>
        </tr>
        <tr class="border-b">
            <th class="text-left py-2 pr-4 font-medium">Date of Birth</th>
            <td class="py-2"><%= new Date(patient.dob).toDateString() %></td>
        </tr>
        <tr>
            <th class="text-left py-2 pr-4 font-medium">Medical History</th>
            <td class="py-2"><%= patient.medicalHistory %></td>
        </tr>
        </tbody>
    </table>
    <div class="flex justify-center mt-6 space-x-4">
        <a href="/patients/edit/<%= patient.id %>" 
            class="px-4 py-2 bg-green-500 text-white rounded hover:bg-green-600 transition">
            Edit
        </a>
        <a href="/patients/notes/view/<%= patient.id %>" 
            class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition">
            View Notes
         </a>
        <a href="/patients/appointments/add/<%= patient.id %>" 
            class="px-4 py-2 bg-gray-500 text-white rounded hover:bg-gray-600 transition">
            Display Appointments
        </a>
    </div>
  </div>

</body>
</html>
